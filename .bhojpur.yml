# List the start up tasks. You can start them in parallel in multiple terminals. See https://docs.bhojpur.net/config-start-tasks/
tasks:
  - init: |
      sudo apt install shellcheck
    command: |
      brew install gh
      gh extension install .
      shellcheck gh-bp
vscode:
  extensions:
    - timonwong.shellcheck
